# Cloudflare Origin Certificate goes here (git-ignored):
#   origin.pem  — the Origin Certificate
#   origin.key  — the Private Key
# Create via Cloudflare dashboard → SSL/TLS → Origin Server → Create Certificate.
